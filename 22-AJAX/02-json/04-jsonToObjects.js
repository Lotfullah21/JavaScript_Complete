// Crucial point is the whole data should be formatted as a string.
const jsonData = `{ "name": "Ahmad", "age": 20, "location": "Afghanistan" }`;
console.log("JSON to Js Objects =", JSON.parse(jsonData));
