/* same as while loops, but the condition will be at last points..
it runs atleast  one
*/
let summation = 0;
do {
  console.log(`You have ${summation} Afghani`);
  summation += 1;
} while (summation < 10);

// Runs atleast once
let x = 11;
do {
  console.log("hello", x);
  x--;
} while (x > 100);
