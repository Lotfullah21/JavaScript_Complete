// Checks if the item is in the array or not

const subs = ["dl", "ml", "py"];
const res = subs.includes("dl");
console.log(res);

// const res = subs.includes("dl","starting inde");
const newRes = subs.includes("dl", 1);
console.log(newRes);
